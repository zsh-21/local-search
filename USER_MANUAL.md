# File Search 使用手册

## 1. 软件简介
File Search 是一款专为 Windows 设计的轻量级、高性能本地搜索工具。它结合了极速文件索引与应用启动功能，提供流畅的现代化交互体验，支持高度个性化定制，助您从繁杂的文件管理中解放出来。

---

## 2. 核心功能

### 🔍 极速检索
*   **全盘索引**：毫秒级响应，实时检索本地文件与文件夹。
*   **应用启动**：自动扫描已安装程序，支持通过名称快速启动应用。
*   **智能匹配**：支持模糊搜索，无需精准输入全名即可找到目标。

### 🚀 快捷列表（智能历史）
*   **自动记录**：软件会自动记录您最近打开的文件或应用，搜索框为空时直接展示，方便快速访问高频项目。
*   **右键集成**：在 Windows 资源管理器中，选中任意文件 -> **右键** -> 选择 **“添加到FileSearch的快捷列表”**，即可将其手动固定到搜索首页。

### 🏷️ 类型过滤
*   **内置类型**：支持快速切换“所有”、“文件”、“文件夹”视图。
*   **自定义扩展名**：支持在设置中添加自定义文件类型（如 `ext:png`, `ext:doc`），实现精准的格式筛选。

---

## 3. 交互指南

### ⌨️ 快捷键操作
*   **呼出/隐藏**：`Alt + T`（默认，可修改）。
*   **打开设置**：`Alt + Shift + T`（默认，可修改）。
*   **关闭窗口**：`Esc`。
*   **结果选择**：`↑` / `↓` 方向键。
*   **确认打开**：`Enter`。
*   **切换类型**：`Tab` 键循环切换搜索类型。

### 🖱️ 鼠标交互
*   **智能穿透（特色功能）**：
    *   当鼠标移出搜索框的有效交互区域时，窗口背景自动透明，鼠标点击可直接穿透至桌面底层（不遮挡视线）。
    *   当鼠标移回搜索框时，自动恢复交互状态。
*   **窗口拖拽**：按住搜索框空白区域或边缘，即可拖拽移动窗口位置。
*   **结果操作**：
    *   **单击**：直接打开文件/应用。
    *   **功能按钮**：每条结果右侧提供 **“打开所在文件夹”** 和 **“复制路径”** 按钮。
    *   **删除记录**：在历史模式下，可点击右侧删除按钮移除单条记录。

---

## 4. 个性化设置

进入设置页面（点击搜索框右侧齿轮图标或使用快捷键），您可以：

### 🎨 外观定制
*   **主题切换**：支持 **深色 (Dark)** / **浅色 (Light)** 模式。
*   **多彩配色**：内置 10 种强调色（如天际蓝、罗兰紫、极光绿等），随心搭配。

### ⚙️ 搜索偏好
*   **搜索状态保持**：开启后，关闭再重新呼出搜索框时，将保留上次输入的文字和结果，方便连续操作。
*   **开机自启**：支持随 Windows 启动自动后台运行。
*   **历史记录管理**：可调整历史记录数量上限，或一键清除所有历史。

### 🛠️ 高级配置
*   **自定义搜索类型**：
    *   输入扩展名（如 `md`, `psd`）添加专属筛选标签。
    *   **拖拽排序**：长按拖拽设置页中的类型标签，可调整其在搜索主界面的显示顺序。

---

## 5. 常见问题 (Q&A)

*   **Q: 为什么搜不到某些系统文件？**
    *   A: 为保证性能和结果相关性，软件默认过滤了 `Windows`、`node_modules`、`.git` 等系统或开发目录。
*   **Q: 如何重置软件数据？**
    *   A: 在设置中清除历史记录，或者手动删除用户数据目录下的配置文件即可。
