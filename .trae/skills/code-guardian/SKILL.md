---
name: code-guardian
description: 生成代码时强制调用此技能，强制执行严格的代码修改与生成规则：以用户手动修改为不可回退基线、仅做最小改动、全程使用中文，确保生成的代码无报错、高复用、易维护且符合生产环境提交标准。
---

# 代码守护者 (Code Guardian)

此技能为生成代码时的强制调用技能，强制执行严格的代码生成与修改指南，确保生成的代码达到生产环境提交标准，同时尊重用户手动修改的基线并保持中文使用的一致性。

## 核心规则

1.  **代码零报错原则 (Zero Error Principle)**：
    *   **必须**确保生成/修改后的代码能够通过 TypeScript/Linter 检查。
    *   **严禁**引入未使用的变量、未定义的类名或类型、过时的导入等影响代码提交的问题。
    *   在提交任何修改前，必须自检是否满足“直接提交代码”的要求。

2.  **强制代码注释 (Mandatory Code Comments)**：
    *   **每一次**新增或修改任何代码时，**必须**同步补充中文注释，解释“这段新增/修改代码做了什么、为什么要这么做”。
    *   注释应**贴近改动点**（函数/逻辑块上方或关键分支旁），避免只在文件顶部写泛泛说明。
    *   避免“废话注释”（例如逐行翻译语法）。优先写：业务意图、关键边界条件、与旧逻辑的差异点、潜在风险与假设。
    *   在不破坏可读性的前提下，**尽量**用“一段改动对应一段注释”的粒度；不要为了凑注释而把代码切碎。

3.  **组件拆分与规范化 (Component Decoupling & Structure)**：
    *   **必须**保持代码的可复用性。能够拆分成独立组件的逻辑应主动拆分到新文件中。
    *   **图标与 SVG 资源**：必须放置在专门的文件夹下（如 `src/assets/icons` 或 `src/components/icons`），不得在业务代码中内联超长 SVG。

4.  **健壮性与可扩展性 (Robustness & Extensibility)**：
    *   编写代码时必须考虑边界情况、错误处理以及未来的扩展性。
    *   遵循 SOLID 原则，确保代码易于维护和测试。

5.  **中文优先原则 (Chinese Language First)**：
    *   所有的**注释**、**文档**、**描述**以及**与用户的对话**，必须使用**中文**。

6.  **手动修改保护原则 (User-Edited Protection)**：
    *   **必须**将用户已手动修改的内容视为“不可回退的基线”，禁止覆盖、回滚或用“更优雅写法”替换其实现。
    *   **严禁**对非相关文件或非相关代码做顺手式调整（包括但不限于：格式化、重命名、抽公共方法、替换写法、移动文件）。
    *   **默认**优先在既有文件与既有结构内完成目标；仅当“无法在现有结构实现”时，才允许新建文件或拆分组件。
    *   **禁止**无明确需求地新增文档类文件（如 README、说明文档等）或为了“补全规范”而引入与任务无关的改动。

7.  **复用率优先原则 (Reuse-First Principle)**：
    *   **必须**优先提升复用率：同类 UI/交互/校验/格式化逻辑出现第二次时，就应抽离为可复用模块（组件/Hook/工具函数/类型），避免重复实现。
    *   **禁止**复制粘贴堆叠：允许为满足“最小改动”短期保留一次性写法，但一旦重复趋势明确（同一逻辑在 2+ 处出现或即将出现），必须在**当前需求范围内**完成抽象。
    *   复用抽象必须可读可用：API 尽量小而稳定，默认值合理，边界条件完整，并与项目现有模式一致。
    *   模块边界必须清晰：组件负责 UI，Hook 负责状态/副作用，工具函数负责纯逻辑；跨模块依赖保持单向与低耦合。

8.  **问题代码替换原则 (Replace-over-Accumulate Principle)**：
    *   **必须**在修复功能时删除原有有问题的实现或冗余分支，不允许通过叠加新分支“绕过旧逻辑”。
    *   **必须**同步清理已废弃的常量、导入、配置和调用路径，避免代码量持续膨胀。
    *   若出于兼容性需要临时保留旧逻辑，必须满足“可回收、可识别、可在后续一轮移除”，并优先给出直接替换方案。

## 使用说明

*   **生成/修改代码时必须强制激活并调用此技能，无任何例外场景。**
*   在涉及界面变动时，必须检查 `App.css` 中的变量定义，确保支持主题切换。
*   在应用变更后，必须使用 `GetDiagnostics` 等工具验证代码是否存在 Linter 错误。
*   在规划变更时，显式检查并确认没有触碰无关的函数或行。
*   如果发现“最佳实践”与“用户的手动修改”存在冲突，**必须**遵循用户的手动修改。

### 代码示例（必须包含中文注释）

```ts
// 将字符串安全地裁剪到指定长度：空值返回空字符串，超长时追加省略号
export function truncateText(input: string | null | undefined, maxLength: number): string {
  const value = input ?? ""

  // 边界：maxLength 非法时直接返回原值，兼容业务中传入非合规数值的场景
  if (!Number.isFinite(maxLength) || maxLength <= 0) return value

  if (value.length <= maxLength) return value

  return `${value.slice(0, maxLength)}…`
}